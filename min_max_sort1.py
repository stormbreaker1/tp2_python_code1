# min, max and sort

# This is our list.
our_list = {
        'meliodas':60000,
        'elizabeth':59999,
        'king':20000,
        'diana':19000
}

# Now, we will zip using zip() as if keys and values are two different lists.
# min, max and sort will be performed on the basis of first parameter passed
 # inside the zip()
 
 print(zip(our_list.keys())